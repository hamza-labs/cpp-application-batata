#pragma once

enum EnumDirection
{
    DIR_UP = 's', // Taper 's' pour déplacer le joueur vers le haut
    DIR_DOWN ='x', // Taper 'x' pour déplacer le joueur vers le bas
    DIR_LEFT = 'w', // Taper 'w' pour déplacer le joueur vers la gauche
    DIR_RIGHT = 'c' // Taper 'c' pour déplacer le joueur vers la droite
};
